# 验证记录

验证日期：2026-09-12。环境：Apple Silicon arm64，macOS 26.6.2，Clash Verge Rev 2.5.2 / Mihomo v1.19.29。

## 自动化测试

执行 `python3 -m unittest discover -s tests -v`，**31 项通过**。

- 首次基线、重复快照、活动连接消失、旧连接不补算、新连接开始时间。
- 直连/拦截排除，无法判断的路由不混入代理用量。
- 断连、计数回退、时钟回退；跨午夜分桶保持上传与下载字节守恒。
- 切换订阅时已建立连接保留原归属；同名不同身份节点正确区分。
- 应用辅助进程归并、未知应用标签。
- 分钟/15 分钟/小时/天查询，任意分钟范围结束时刻排除。
- 数据重新打开后保留；汇总、明细与筛选；手动映射与恢复自动识别。
- 指定区间清理只删除区间数据；保留策略删除旧记录。
- 注入超过 8 MB 的测试数据库，确认按大小删旧并压缩到上限内，最近数据保留。
- CSV 对公式前缀转义；无效设置、无效节点和日期范围被拒绝。
- HTTP Host、Origin、CSRF 校验，未确认清理请求拒绝，资源路径越界拒绝。

## 页面与桌面验证

- 使用真实本机流量，不注入演示数字。
- 应用筛选后明细只显示所选应用；自定义 `12:00–13:00` 查询与分钟粒度生成 60 个桶，URL 保持范围和粒度。
- 检查桌面和 390px 窄窗口；修复辅助隐藏标签造成的横向溢出，文档宽度与视口均为 390px。
- 独立 `.app` 成功打开 Cocoa / WebKit 窗口，不启动外部浏览器。
- `⌘E` 显示 macOS NSSavePanel；未向用户目录写入测试导出文件。
- `⌘,` 正确打开存储设置，显示真实占用、180 天 / 256 MB 默认设置及时间范围清理入口。
- 真实历史从预览记录器迁移到后台服务，再切换到应用内封装记录器，累计数字未减少。
- 已验证 LaunchAgent 当前运行程序为 `.app/Contents/Resources/service/clash-traffic-service`，不再依赖开发 Python 环境。
- 修复旧进程退出时序后，正式内置服务的 `manage.py restart` 已通过，接口恢复正常。
- 数据库 `PRAGMA integrity_check` 为 `ok`，权限为 `0600`。

## 安装包

- Cocoa 主程序与独立记录器均为 Mach-O arm64。
- 完整 `.app` 大约 11 MB；DMG 包含 `.app`、Applications 链接及安装说明，不含本机历史、订阅配置或身份密钥。
- `codesign --verify --deep --strict` 对安装目录及挂载安装盘内的应用均通过。
- `hdiutil verify` 镜像 CRC 校验通过；只读挂载检查后已弹出测试挂载。
- 配套 SHA-256 文件位于安装包旁。

## 未验证及局限

没有重启整台 Mac 或实际注销账户来测试登录事件；已检查 LaunchAgent 的 RunAtLoad、KeepAlive 和当前运行状态。未验证 Intel Mac、旧版 macOS、其他 Clash 客户端、复杂 Provider 更新、多订阅多跳或受额外安全策略限制的系统。

应用采用本机 ad-hoc 签名，没有 Apple Developer ID 签名、公证或自动更新。快照采样的短连接和连接末尾遗漏属于已知统计限制，不因测试通过而消失。

## 0.1.1 界面精简（2026-09-12）

- 删除顶部面包屑、重复页面标题、英文装饰和说明段落，首页直接呈现筛选与统计。
- 菜单 18px、主要正文 16px、卡片标题 22px、流量数字最高 46px；弹窗采用同一字号体系。
- 自定义日期时间按需展开，导出入口移至明细，详细口径保留在统计说明中。
- macOS 原生窗口实测首页、日期展开和存储设置；保留原有数据库与清理设置，后台恢复采集。
- 31 项 Python 测试通过，JavaScript 语法检查通过。
- 修复重复打包时 Python 框架符号链接覆盖失败；构建完成后验证应用签名。

## 0.1.2 控件与文档（2026-09-12）

- Flatpickr 4.6.13 本地资源已集成；HTTP 资源白名单测试覆盖 JS、CSS、中文包。31 项 Python 测试通过，JS 语法检查通过。
- 在独立端口 19798、独立临时数据库完成交互验证：手动清理开始时间回填 09:35，清理确认显示相同范围后取消；总览自定义日期提交后 URL 保留 09:35，查询无错误。
- 在 1280×720 浏览器预览验证设置对话框及操作栏均位于窗口内；在本机原生 WKWebView 验证自动/手动页签、日期模态层、完整日历与关闭流程。
- 正式用户数据库没有执行测试清理；升级后后台采集恢复，原保留天数和空间上限保留。
- 使用/运行/系统三份 Markdown 和四页离线 HTML 已生成；文档导航与本地链接已检查。帮助菜单集成对应本地文档。
- 原生应用签名验证通过，DMG 校验通过；包内包含日期组件许可证与离线文档。

## 0.1.3 菜单栏入口（2026-09-12）

- 新增常驻 NSStatusItem、模板图标、本机状态/速率异步轮询、打开窗口和服务管理菜单，以及可保存的图标显隐偏好。
- 关闭最后一个窗口后不终止桌面进程，显式退出仍保留独立后台服务。
- 原生编译和签名验证通过；本机安装后检查了显示菜单新增入口，关闭窗口后原进程仍存活，可重新显示主窗口。
- 本机 HTTP 状态确认后台健康，历史与清理设置保留。DMG 校验通过。
- 当前 UI 工具只返回窗口和应用菜单，未取得系统右侧菜单栏的截图；顶部图标点击菜单与不同菜单栏外观尚未完成截图验证。

## 0.1.4 日历、图表与应用聚合

- 37 项 Python 测试通过：新增组合/原始来源的字节守恒、不同节点身份保留、组合筛选成员、闰年 366 日、日期结束边界、CSV 两种模式、偏好持久化与 HTTP 鉴权校验。
- 浏览器实际交互：本月、本年 12 个月共 365 日、日历点击回到当天小时粒度、柱状/折线切换、原始来源弹窗、应用分别统计、偏好重新加载、聚合规则弹窗。
- 1280 × 720 窗口检查无页面横向溢出，来源弹窗可滚动；折线纵向绘图区与柱状图均为 220 px，缺失时段不连线。
- 测试使用独立临时数据目录，不清理正式历史。真实历史的同名但不同编号保留，可核对原始代理链。

- 已安装 0.1.4，正式记录器 connected/alive；升级前历史行数、上传及下载合计均保留。原生窗口已验证月日历；应用签名与 DMG 校验通过，帮助目录包含聚合规则。

## 0.2.0 Next.js + shadcn/ui 重构验证（2026-09-12）

- Next.js 16.3.5 静态生产构建通过：总览、明细、归属、存储、说明 5 个业务路由。
- TypeScript 检查通过；7 项前端业务测试通过（北京时间月/年/闰年边界、日期范围、URL 偏好、采集覆盖、颜色阈值、身份区分、图表纵轴）。
- 40 项 Python 测试通过；原有增量计数、排除直连、合并流量守恒、清理、CSV、HTTP 安全测试继续通过。新增资源路由、CSP 哈希、MIME、路径与符号链接隔离测试。
- 浏览器验证：全年 12 个月/365 日期，日期筛选随路由保留；柱状/折线切换；原始来源展开；应用合并/拆分；日期弹层选择与回填；保存策略及手动清理的确认/取消；无页面错误和 CSP 错误。测试未执行正式历史清理。
- 独立 Cocoa/WKWebView 预览窗口验证页面实际渲染、菜单导航、⌘, 设置页以及 ⌘E 原生 CSV 保存对话框。预览只连接独立端口与测试数据目录，未替换正式安装或接管正式 LaunchAgent。
- 打包后的 Python 服务在独立目录和端口启动成功：静态首页、各路由、离线文档和偏好 API 返回成功，采集线程存活；`.app` 的 ad-hoc 签名校验通过。
- 构建目标为 Apple Silicon，最低系统声明 macOS 13.3；本机 macOS 26.6.2 验证。未验证其他电脑和旧系统；尚未做 Developer ID 签名或公证。

0.2.0 DMG 已生成并通过 `hdiutil verify`，SHA-256 校验文件随包提供。正式安装保持原版本运行；本次交付为可安装的新包，临时预览服务与窗口已关闭。

### 0.2.0 顶部筛选栏固定修正

- 总览与明细共用筛选栏采用 sticky 定位，背景与分隔线遮挡下方滚动内容。
- 1280 × 720 浏览器验证：总览滚动 1442 px、明细滚动 650 px，筛选栏 top 均为 0；无横向溢出，滚动后下拉选项与本月切换正常，无控制台错误。
- Next.js 生产构建与 TypeScript 检查通过；桌面应用和同名 0.2.0 DMG 已重新构建、签名校验与 DMG 校验通过。正式安装未替换。
